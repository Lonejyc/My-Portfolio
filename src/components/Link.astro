<div class="link"><slot /></div>

<style>
	.link {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--text-color-1);
		border: 1px solid var(--accent-1bis);
		background-color: var(--accent-1bis);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
	}
</style>