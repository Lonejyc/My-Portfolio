<div class="link"><slot /></div>

<style>
	.link {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--text-color);
		border: 1px solid var(--accent-bis);
		background-color: var(--accent-bis);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
	}
</style>